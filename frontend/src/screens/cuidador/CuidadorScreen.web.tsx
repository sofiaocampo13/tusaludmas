import React from 'react';
import { View, Text } from 'react-native';

export default function CuidadorScreenWeb() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 20 }}>Acceso restringido: Usa la App Móvil para esta sección.</Text>
    </View>
  );
}